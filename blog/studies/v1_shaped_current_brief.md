# Study Brief: `v1-shaped-current`

## Claim

Draft claim for confirmation:

> Under the current shaped setup, do we get robust, measurable collective structure that improves survival?

This wording is intentionally narrow. It does not claim minimal emergence, mechanism identification, or generalization beyond the frozen setup.

## Frozen Environment Version

- Study ID: `v1-shaped-current`
- Config: `configs/studies/v1_shaped_current.toml`
- Environment class: `VectorMurmurationEnv`
- Observation definition: 18D prey observation and centralized critic state exactly as implemented in the week-1 frozen config
- Reward definition:
  - survival reward `+0.1`
  - collision penalty `-2.0` per collision neighbor
  - death penalty `-100.0`
  - PBRS boundary weight `1.0`
  - PBRS density weight `1.0`
  - predator catch reward `+10.0`
  - predator hunger penalty `-0.05`
- Curriculum definition:
  - predator catch radius anneals from `2.0` to `0.5` over `5,000,000` environment steps
- Motion definition:
  - prey base speed `5.0`
  - prey min speed `2.5`
  - predator sprint multiplier `1.5`
  - predator turn multiplier `1.5`

## Evaluation Metrics

Every candidate run should be summarized by the fixed report generated by `analyze_run.py`.

Required scorecard:

- prey survival fraction over time
- predator capture rate per step
- nearest-neighbor distance distribution
- local-density distribution
- polarization / heading alignment
- connected-component count under a distance graph
- swarm radial spread from center of mass
- fringe / edge exposure fraction

## Baseline Ladder

Named baseline configs live under `configs/experiments/baselines/`.

Week-1 baseline set:

1. `random_vs_random.toml`
2. `learned_prey_vs_random_predator.toml`
3. `random_prey_vs_learned_predator.toml`
4. `learned_prey_vs_learned_predator.toml`
5. `heuristic_boids_vs_random_predator.toml`
6. `learned_prey_no_predator_pressure.toml`

Checkpoint-backed baselines are intentionally explicit: the config exists now, and the exact checkpoint path must be supplied before week-2 execution.

## Seed Policy

- exploratory comparison: `2-3` seeds
- candidate benchmark claim: `5` seeds
- external review package: `8-10` seeds

Week 2 should begin with `3` seeds for the baseline ladder, then only expand once the first scorecards are reviewed.

## Exit Criteria For Week 1

- the frozen study version is represented by a config file, not only by `runner.py`
- the claim can be read in one sentence from this brief
- the baseline ladder has named configs
- every run can emit the same metric report schema
- every new run can write a manifest and registry entry
